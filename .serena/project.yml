project_name: "EnterpriseApp"

ignore_all_files_in_gitignore: true

ignored_paths:
- "**/bin/"
- "**/obj/"
- "**/*.user"
- "**/Migrations/"
- "**/.vs/"

read_only: false

excluded_tools: []

initial_prompt: |
  EnterpriseApp es una plantilla Clean Architecture en .NET 9 / C# 13 con PostgreSQL | SQL Server | SQLite,
  Redis (HybridCache), MediatR 12, EF Core 9, JWT y .NET Aspire.
  Lee EnterpriseApp/CLAUDE.md para la visión general. El detalle profundo está en las memorias Serena; cárgalas
  BAJO DEMANDA (no todas de golpe): 'arquitectura', 'esquema-db', 'modulos', 'convenciones'.
  Usa herramientas simbólicas antes de leer archivos completos; la memoria orienta, el código manda.

encoding: utf-8

languages:
- csharp

default_modes:

added_modes:

included_optional_tools: []

fixed_tools: []

symbol_info_budget:

language_backend:

line_ending:

read_only_memory_patterns: []

ignored_memory_patterns: []

ls_specific_settings: {}

ls_workspace_folders:
- .

ls_additional_workspace_folders: []

activation_command:

activation_command_timeout: 180.0
